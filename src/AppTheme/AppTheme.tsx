import Colors from "../Constants/Colors";
import AppImages from "../Constants/Images";
const AppTheme = {
    primaryColor: Colors.primary,
    secondryColor: Colors.secondary,
    btnPrimaryColor: Colors.primary, 
};