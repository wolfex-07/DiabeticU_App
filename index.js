/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import DiabeticUApp from './DemoApp';

AppRegistry.registerComponent(appName, () => DiabeticUApp);
